using System;

namespace KataWordWrap
{
    public class Wrapper
    {
        public string Wrap(string text, int maxLineLength)
        {
            /*
             * Solution design to wrap short words (s. flow 1 and 2):
             * 1. Split line into words
             * 2. Build lines from words
             */
            return text;
        }
    }
}